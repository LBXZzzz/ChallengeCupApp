<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MRRA" parent="Theme.Material3.DynamicColors.Dark.Rikka">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="titleTextColor">@color/white</item>
        <item name="itemTextColor">@color/soft_white</item>

        <item name="colorPrimary">@color/light_theme_dark_mode</item>
        <item name="colorPrimaryDark">@color/dark_theme_dark_mode</item>

        <item name="backgroundColor">?android:colorBackground</item>
    </style>
</resources>