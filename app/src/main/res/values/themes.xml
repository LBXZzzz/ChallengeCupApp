<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MRRA" parent="Theme.Material3.DynamicColors.Light.Rikka">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:statusBarColor">?android:colorBackground</item>

        <item name="titleTextColor">@color/black</item>
        <item name="itemTextColor">@color/soft_black</item>

        <item name="colorPrimary">@color/light_theme_light_mode</item>
        <item name="colorPrimaryDark">@color/dark_theme_light_mode</item>

        <item name="backgroundColor">?android:colorBackground</item>
    </style>
</resources>